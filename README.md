```bash
make configure  # configures environment, environment variables, etc.
make install    # download vendor libs
make -j2        # builds codebase
make run        # start server. could also be `make daemon` for background job
```
